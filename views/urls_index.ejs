<!DOCTYPE html>
<html lang="en">
  <head></head>
  <body style="font-size:20px; padding:50px;">
    <% include ../partials/_header %>
    <ul>
      <% for (let urlListing in urls) { %>
        <li><strong><%= urls[urlListing] %></strong> –––– <em><%= urlListing %></em></li>
      <% } %>
    </ul>
    <a>Create new Tiny URL</a>
  </body>
</html>